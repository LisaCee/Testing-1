//mocks a duplicate of a unit of code that has no implementation
// mock an api call
// // if your code uses an api call
// //jest will automatically insert the api call for me. 
// // __mocks__ folder - would have utilitiesMocks

// const fakeApiCall = () => {
//     setTimeout(()=> {
//         return {
//             data: {
//                 users: 'dan'
//             }
//         }
//     })
// }

// const axios = {
//     get: () => {
//         setTimeout(()=>{
//             return Data:
//         })
//     },

//     post: (address, body, headers) => {

//     }
//     // fake api calls
// }
// the API call will pull this function instead of actually calling an API
// make a fake logdash axios. and make fake functions that uses
// the same API and will return the value with the type and shape that they return

